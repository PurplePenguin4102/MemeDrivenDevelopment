<p>Welcome <%= @email %>!</p>

<p>You can confirm your account email through the link below:</p>

<p><%= link_to 'Confirm my account', ::Rails.application.config.hard_coded_subir + confirmation_url(@resource, confirmation_token: @token) %></p>
